"""xrd_toolkit.gui —— PySide6 桌面前端（在建）。

主框架（与用户讨论定稿，MDI 子窗口式绘图区）：
  - 左侧：文件列（勾选列表 = 唯一选中表达：点行=加选（只勾不取消）、
    点对号方块=勾上/取消；选入/拖入的文件默认全部勾上，重复文件
    弹窗问覆盖/改名（改名有输入框，预填编号名可自定义）；
    点一次作图按钮 = 对勾选文件批量出图）
  - 顶部：工具栏 = [校准] 模式开关（按下 = 校准工作台，弹起 = 默认
    分析工作台）+ [2D][剖面][1D][瀑布] 作图按钮（点一下 = 打开面板
    并计算该视图，纯动作不是开关）+ [对比]（勾选 >= 2 个文件时把
    它们的 1D 曲线叠进同一张图，重复点击 = 刷新该面板）+ 横排/竖排
    一键重排 + [文件][参数][日志] 收起开关（收起 = 给图让地方；也可
    用各坞标题栏的 ×，全收起来就只剩绘图区）
  - 中央：绘图区 = MDI 子窗口区（每张图一个独立子窗口，各拖各的
    互不牵连；每张 1D/对比图工具栏末尾有 [弹出]/[收回] = 把面板
    搬进独立 OS 窗口再收回来）。手动缩放完全自由：拖成什么样就
    什么样；每拖一次 = 记住当前画布比例，没拖过 = 默认画布 5:3
    （500×300）。子窗口边框几乎不可见 → 图自带抓手：四边 5px
    抓取带 + 四角 16px 抓取区（右下角有可见把手），悬停换光标、
    按住拖 = 拉伸；点窗口任何地方（标题栏/边框/图）都选中该面板
    （选中子窗口 = 选中参数）。开新图默认大小级联摆放、完全不动
    旧图；横排/竖排 = 按类型分层摆位置（每类一行/一列，类型顺序
    = 开图先后）：图保持各自大小绝不缩放，放不下靠滚动条看
    （宁可滚动也不压扁），弹出去的窗口不参与。关闭面板（子窗口
    × 或弹出窗口 ×）= 关闭即遗忘：状态全丢，再次打开 = 全新的
    默认图；关软件时的保存询问只列当时还开着的面板。每张 1D
    面板自带精简工具栏 [Home][Customize][Save]（Home = 回初始
    视图，不绑双击）；手势：按住左键拖 = 平移、滚轮（触摸板两指
    滚动）= 以光标为中心缩放（每格 10%，比旧版 25% 温和）。
    缩放/平移/自定义范围的改动实时写回参数面板：视图 2θ 范围 =
    看图的窗口，只显示不参与计算、与数据组的积分 2θ 范围互不干扰
    （[恢复默认] 回到跟随积分范围）；动过纵轴 = 自动纵轴关掉、窗口
    由用户接管。鼠标悬停在曲线上 = 线上出现白边圆点（吸附最近真实
    数据点，颜色跟随所选曲线）+ 状态栏常驻坐标标签实时显示该点
    2θ/强度，对比图坐标前缀 = 文件名，移开即清空
  - 右侧：参数面板（随"最新生成 / 鼠标选中的图"切换——该图成为焦点，
    参数面板即显示它的参数；自动初值可改，点[应用]生效。数据参数
    与显示参数都随焦点面板回放快照（每张图各记各的）：新开面板
    显示参数从默认起步、重算保留自己的旧设置；两个 [应用] 各管各
    的一组参数，且只作用于焦点那张图。坞内上下对半：编辑对象名
    固定最上方，数据/图像参数各占一半、各自独立滚动，[应用] 在
    上 [恢复默认] 在下竖排一列固定在各区最下方；单位随输入框后缀
    显示、成对范围并排一行、小节用灰色标题、每项带悬停提示，坞
    设最小宽高——拖动边框不会把控件裁掉）
  - 底部：日志区（可收起）+ 状态行（坐标框（等宽字体 + 凹槽框）|
    竖分隔线 | 当前文件）
  - 工作台分工：默认 = 分析（outputs：2D / 剖面 / 1D / 瀑布）；
    [校准] 按下 = 校准工作台（定几何：束心/距离/波长，自动+点图微调）

规则：定束心直接自动、不满意点图微调；[保存] 目前 = 导出已出图
面板为 PNG，校准存几何配置 / 图像存积分结果（与 CLI 同格式）待
校准工作台接线时实现；不做会话记忆。

启动：python -m xrd_toolkit.gui
"""
