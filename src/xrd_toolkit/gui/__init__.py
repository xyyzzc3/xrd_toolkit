"""xrd_toolkit.gui —— PySide6 桌面前端（在建）。

主框架（与用户讨论定稿，MDI 子窗口式绘图区）：
  - 左侧：文件列（勾选列表 = 唯一选中表达：点行=加选（只勾不取消）、
    点对号方块=勾上/取消；选入/拖入的文件默认全部勾上，重复文件
    弹窗问覆盖/改名（改名有输入框，预填编号名可自定义）；
    点一次作图按钮 = 对勾选文件批量出图）
  - 顶部：工具栏 = [校准] 模式开关（按下 = 校准工作台，弹起 = 默认
    分析工作台）+ [2D][剖面][1D][瀑布] 作图按钮（点一下 = 打开面板
    并计算该视图，纯动作不是开关）+ [对比]（勾选 >= 2 个文件时把
    它们的 1D 曲线叠进同一张图，重复点击 = 刷新该面板）+ 横排/竖排
    一键重排 + [文件][参数][日志] 收起开关（收起 = 给图让地方；也可
    用各坞标题栏的 ×，全收起来就只剩绘图区）
  - 中央：绘图区 = MDI 子窗口区（每张图一个独立子窗口，各拖各的
    互不牵连；每张图顶上一行自绘标题栏 = 四个按钮 +
    [弹出]/[关闭]（不显示名字：名字在参数坞"编辑对象"和图上标题
    里已有；按住拖 = 移动、双击 = 占满绘图区，原生标题栏已换成
    这一行：壳从 83 px 瘦到 26 px，当前编辑对象那块底色更深）；[弹出]/[收回] = 把面板
    搬进独立 OS 窗口再收回来）。手动缩放完全自由：拖成什么样就
    什么样；每拖一次 = 记住当前画布比例，没拖过 = 默认画布 5:3
    （500×300）。子窗口边框几乎不可见 → 图自带抓手：四边 8px
    抓取带 + 四角 24px 抓取区（右下角有可见把手），悬停换光标、
    按住拖 = 拉伸；点窗口任何地方（标题栏/边框/图）都选中该面板
    （选中子窗口 = 选中参数）。开新图默认大小左上角小错位级联
    （24px 一档、6 档循环，下面几张的标题栏露出来）、完全不动
    旧图；横排/竖排 = 按类型分层摆位置（每类一行/一列，类型顺序
    = 开图先后）：图保持各自大小绝不缩放，放不下靠滚动条看
    （宁可滚动也不压扁），弹出去的窗口不参与；摆图前滚动自动
    归零（否则 Qt 把滚动偏移混进摆图坐标、图越排越漂）。总缩放
    = 绘图区全体同比缩放（50%–200%，每格 10%）：Ctrl+滚轮
    （Excel 习惯）或底部条 − 100% + 按钮，围绕视口中心缩放，
    只动绘图区里的图（弹出去的不参与，平铺也不碰它），新开/
    收回的图按当前总缩放落位。关闭面板（子窗口 × 或弹出窗口
    ×）= 关闭即遗忘：状态全丢，再次打开 = 全新的默认图；关软件
    时的保存询问只列当时还开着的面板。每张 1D 面板自带精简工具
    栏 [Home][Zoom][Customize][Save]（Home = 回初始视图，不绑
    双击）；手势：按住左键拖 = 平移（放大镜点不点亮都是）、滚轮
    （触摸板两指滚动）= 只滚动绘图区；放大镜按钮 = 开关（点亮/熄灭
    可见 + 日志提示）：点亮 = 滚轮以光标为中心缩放（每格 10%，比
    旧版 25% 温和）+ 左键拖 = 框选放大；熄灭 = 滚轮滚动、左键拖
    = 平移。框选放大的选框是自己画的（matplotlib 的 zoom 模式永不
    启用）——2026-09-18 报过的"画完框再缩小、框内数据变小"就是
    旧版用 mpl 框选模式与平移手势抢事件造成的；现在这条路径只改
    坐标范围，一个数据点都不碰。
    缩放/平移/自定义范围的改动实时写回参数面板：视图 2θ 范围 =
    看图的窗口，只显示不参与计算、与数据组的积分 2θ 范围互不干扰
    （[恢复默认] 回到跟随积分范围）；动过纵轴 = 自动纵轴关掉、窗口
    由用户接管。Home = 回到**这张图本来的样子**（记在 dock.view_home：
    首画/重算/[应用] 会刷新它，滚轮缩放与框选平移这些手势不算——
    2026-09-24 用户要求"回到最初的样子，而不是上次画的位置"）；Customize = 自绘轴属性对话框（表单标签
    左对齐、分节排列：标题与轴标签 / 纵轴刻度（线性/对数）/ 图
    边距，[恢复默认][取消][应用]），里面改的归用户——标题/轴
    标签/曲线样式/纵轴刻度重画一律不覆盖，只有参数面板里又改了
    一遍（显示名、[对数纵轴]）才由参数接管。鼠标悬停在曲线上 =
    线上出现白边圆点（吸附最近真实数据点，颜色跟随所选曲线）+
    状态栏常驻坐标标签实时显示该点 2θ/强度，对比图坐标前缀 =
    文件名，移开即清空
  - 右侧：参数面板（随"最新生成 / 鼠标选中的图"切换——该图成为焦点，
    参数面板即显示它的参数；自动初值可改，点[应用]生效。数据参数
    与显示参数都随焦点面板回放快照（每张图各记各的）：新开面板
    显示参数从默认起步、重算保留自己的旧设置；两个 [应用] 各管各
    的一组参数，且只作用于焦点那张图。坞内上下对半：编辑对象名
    固定最上方，数据/图像参数各占一半、各自独立滚动；[恢复默认]
    左 [应用] 右并排一行（各占一半宽度）固定在各区最下方；几何
    配置只占下拉框一格，完整批次备注挂在悬停提示上；对比归一化
    = 四选一下拉框（各自最强峰/全图最强峰/指定数据…/不归一化
    （默认），指定数据时旁边出现文件下拉框）；单位随输入框后缀显示、成对
    范围并排一行、小节用灰色标题、每项带悬停提示，坞设最小宽高
    ——拖动边框不会把控件裁掉）
  - 底部：日志区（可收起）+ 状态行（坐标框（等宽字体 + 凹槽框）|
    竖分隔线 | 当前文件）
  - 工作台分工：默认 = 分析（outputs：2D / 剖面 / 1D / 瀑布）；
    [校准] 按下 = 校准工作台（定几何：束心/距离/波长，自动+点图微调）。
    校准工作台已接线（2026-09-19）：参数坞翻成校准页（模式单选 +
    自动/手动按钮区 + 结果三列 自动|手动|Δ偏差 + CONFIGS 模板 +
    复制按钮），中央开出校准图面板（imshow + 理论环圆圈，只接鼠标
    点击）。两种模式二选一或都跑：自动 = 后台跑「自动定环心 → pyFAI
    精修」；手动 = 图上点 ≥3 个点、覆盖 ≥2 个不同环（点自动吸附最近
    理论环 ±0.5°，环号标在图上），pyFAI 反推几何与自动结果并列显示
    偏差。共用保存机制 = CONFIGS 条目模板 + 复制按钮（不写 config.py，
    沿用 CLI 人工复核规矩）；关闭面板/退出模式 = 校准状态清零。标样
    = 文件列表勾选的第一个文件；没勾文件进模式只记日志提示。

规则：定束心直接自动、不满意点图微调；[保存] 目前 = 导出已出图
面板为 PNG，校准存几何配置（模板复制）/ 图像存积分结果（与 CLI
同格式）待 2D/剖面/瀑布接线时实现；不做会话记忆。

模块结构（2026-09-18 从 3622 行单文件起分三轮拆出：09-23 拆校准 /
文件坞 / 配置条目 / 存图，09-24 拆多文件视图与面板壳；调用方向永远
从上往下，反向调用一律函数内延迟导入，全包没有模块级环）：
  app.py          窗口组装：文件坞 / 参数坞 / 日志坞 / 工具栏 / 保存关窗
  plot_compare.py 多文件视图：对比面板（多条 1D 叠图）+ 热图 + 锚点拾取
  plot_views.py   单文件视图：runner 分发 + 四个视图的出图 + 任务回调
  plot_panels.py  面板壳：画布容器 / 半截工具栏 / 手势 / 轴同步 / 悬停
  plot_export.py  导出：1D 数据（txt/chi + CSV 总表）+ 图片（PNG/TIF）
  file_dock.py    左侧文件坞：文件列表 + 打开 / 拖放 + 显示名去重
  calib.py        校准工作台：自动校准 + 手动选点校准
  calib_model.py  校准页纯逻辑（零 Qt）：状态模型 + 采纳判据 + 取值
  calib_panel.py  中央校准图面板：图像 + 理论环（青线）+ 选点交互
  calib_table.py  校准页三列表：当前配置 / A / B + Δ 行 + 基准
  config_ops.py   几何配置条目的进出口：加载 / 保存 .poni、删条目
  panels.py       面板容器生命周期：子窗口 / 弹出 / 抓手 / 平铺 / 总缩放
  customize.py    Customize 自绘轴属性对话框
  panel_state.py  共享层：日志 / 内容入口 / 参数快照 / 焦点回放 / 几何收集
  tasks.py        后台任务运行器（耗时计算挪出界面线程）
扩展点：新视图（2D/剖面/瀑布）接线 = 往 plot_panels 的
_VIEW_BUILDERS 与 plot_views 的 _VIEW_RUNNERS 两张表加条目；app.py
对拆分前的全部函数保留兼容再导出（测试经 gui_app 继续访问，
mock.patch 目标请指到实现所在的新模块）。

启动：python -m xrd_toolkit.gui
"""
